--- # test 설정
spring:
  config:
    activate:
      on-profile: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

--- # local 설정

spring:
  config:
    activate:
      on-profile: local
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/lol_judge
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

--- # production 설정

spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: ENC(SrtaLqhl26qDQ+6az6odL/ziDJDcZx6BhEuKUN9mgqu+loMLKNte1AqkIzYUHmlyXUeypqmzkBtboEujrpJT0g==)
    username: ENC(quODt+czvStLIlogJzjHfDC2SKQ7J+an)
    password: ENC(FLy/X68KFKHi4ux/sdJMJw==)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
logging:
  level:
    org:
      springframework: debug
