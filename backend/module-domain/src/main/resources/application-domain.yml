--- # test 설정
spring:
  config:
    activate:
      on-profile: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

--- # local 설정

spring:
  config:
    activate:
      on-profile: local
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/lol_judge
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

--- # production 설정

spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: ENC(H+o9yzsGPtI6pfnOcXUXzrvIZ3jVZ0HQvUvzRZVKcP2hb4/PBf6ISqZ1Sh08iCwvAU5VeBuXkvKg6kVwUnqPHw==)
    username: ENC(j1yj/M8OgSDufwa57sMUOm4wViGN/qUO)
    password: ENC(W2NqN448mcbV7jWVG1GqVg==)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
logging:
  level:
    org:
      springframework: debug
