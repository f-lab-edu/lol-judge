--- # test 설정
spring:
  config:
    activate:
      on-profile: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

--- # local 설정

spring:
  config:
    activate:
      on-profile: local
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/lol_judge
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

--- # production 설정

spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: ENC(dzWegWTt1CekCaoYg0SyAscmzQRO9IJnFsLnVHcj4zS3h0qeXvKBjQYcOcnmY6/DxUZ0Zn15nVY==)
    username: ENC(EQBBD+ZWgVjfrBSIGJPCEg==)
    password: ENC(6A+EhXxAbOy4+FoLhKUS+g==)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
logging:
  level:
    org:
      springframework: debug
