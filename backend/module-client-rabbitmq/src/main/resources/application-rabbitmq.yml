--- #local and test

spring:
  config:
    activate:
      on-profile: local, test

  rabbitmq:
    host: localhost
    port: 5672
    stream:
      username: guest
      password: guest


--- # production

spring:
  config:
    activate:
      on-profile: production

  rabbitmq:
    host: ENC(tZKUFfG/TeFuAbOqAcTCU3DVV8dAHYu6)
    port: 5672
    stream:
      username: ENC(5NAFBDDUhg+ntmDT5LivpA==)
      password: ENC(NFrHDAiq4smC0IUZjFhS9g==)

