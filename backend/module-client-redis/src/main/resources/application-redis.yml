--- # local 설정

spring:
  config:
    activate:
      on-profile: local
  data:
    redis:
      port: 6379
      host: localhost

--- # production 설정

spring:
  config:
    activate:
      on-profile: production
  data:
    redis:
      port: 6379
      host: ENC(iUIGzMckhc2u7ZkB9n9noegMyPFFpBwx)
