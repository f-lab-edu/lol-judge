<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-/mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.flab.notification.repository.NotificationMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification(member_id, contents, created_at)
        VALUES (#{memberId}, #{contents}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE
        FROM notification
        WHERE id = #{id}
    </delete>

    <select id="findAll">
        <include refid="selectNotification"/>
        WHERE id <![CDATA[<=]]> #{countOffset}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <select id="findNotReadNotifications">
        <include refid="selectNotification"/>
        WHERE id <![CDATA[<=]]> #{countOffset}
        AND read_status = 'NOT_READ'
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <sql id="selectNotification">
        SELECT id, member_id, contents, read_status, created_at
        FROM notification
    </sql>
</mapper>
